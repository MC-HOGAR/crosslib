/* Shared types for modules InternalUser, CustomerUser, SalesUser */
export type UserType = 'Internal' | 'Customer' | 'Sales'

export enum UserTypeEnum {
    Internal = 'Internal',
    Customer = 'Customer',
    Sales = 'Sales',
}